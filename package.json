{
    "name": "subgraph-ens",
    "version": "0.1.0",
    "repository": "https://github.com/KodexLabs/subgraph-ens",
    "license": "MIT",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "build:mainnet": "graph build --network mainnet",
        "create:local": "graph create --node http://localhost:8020/ kodexlabs/ens",
        "deploy:ethereum:mainnet": "graph deploy --product hosted-service kodexlabs/ens-mainnet",
        "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kodexlabs/ens",
        "remove:local": "graph remove --node http://localhost:8020/ kodexlabs/ens",
        "test": "graph test"
    },
    "devDependencies": {
        "@ensdomains/content-hash": "^2.5.3",
        "@graphprotocol/graph-cli": "^0.23.2",
        "@graphprotocol/graph-ts": "^0.23.1"
    }
}
